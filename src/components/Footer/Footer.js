
import {Paper, Typography} from "@mui/material";
import { Box } from "@mui/system";

function Footer() {
    return(
        <Box>
             <Typography>GGE</Typography>
        </Box>
    );
}

export default Footer;